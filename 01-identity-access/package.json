{
  "name": "rlst-01-identity-access",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Module 1 — Identity & Access (IDOR). Ships deliberately vulnerable.",
  "scripts": {
    "dev": "nodemon --legacy-watch --watch src --ext ts,js --exec \"tsx src/server.ts\"",
    "start": "tsx src/server.ts",
    "generate": "tsx src/generate.ts",
    "prisma:push": "prisma db push --skip-generate",
    "check": "node checker/check.mjs",
    "setup": "node setup.mjs"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.8",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "nodemon": "^3.1.9",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
